# Agent 工作流指南（本地改代码 + SSH MCP 远端执行）

## 目标
在本地使用 AI 编码，但所有运行/测试结果以远端局域网服务器为准。

## 环境前提
- 本地笔记本可访问 AI 服务。
- 远端服务器承载运行环境、依赖与模型权重。
- 本地仓库是轻量副本，执行前必须先同步到远端。
- 可通过 SSH MCP 在远端执行命令。

## 强约束规则（必须遵守）
1. 不允许把本地运行结果当作最终正确性依据。
2. 不允许直接在远端手改代码，所有修改必须发生在本地。
3. 每次有意义的本地改动后，必须先同步再执行远端命令。
4. 测试/运行必须通过 SSH MCP 在远端执行。
5. 远端失败时，必须回到本地修复后重复“同步 -> 远端执行”循环，直到通过。

## 标准循环（必须按顺序）
1. 本地修改代码。
2. 同步到远端：
   - `ops/remote sync`
3. 通过 SSH MCP 的 `execute-command` 在远端执行命令。
   - 具体执行什么测试命令、运行命令由 Agent 根据任务自行决定。
   - 不在本文件中硬编码 quick/full 的具体命令模板。
   - 唯一强约束：所有远端执行命令必须走 SSH MCP，不得绕过。
4. 解析标准输出标记：
   - `[RESULT] PASS|FAIL`
   - `[EXIT_CODE] <n>`
   - `[RUN_ID] <id>`
5. 若失败（FAIL）：
   - 提炼远端错误关键信息。
   - 回到本地修复。
   - 重新从第 2 步开始。
6. 若成功（PASS）：
   - 继续下一任务或进入交付。

## 执行策略
- Agent 根据改动范围与任务风险，自主选择合适的远端测试/运行命令。
- 可以先做小范围验证，再按需要升级到更全面验证。
- Bug 修复任务只有在远端验证通过后才能宣称完成。

## 异常处理
- 同步失败：先修复 SSH/权限/路径/冲突问题，不得跳过同步直接运行。
- MCP 执行失败：先重试一次，仍失败则明确报告阻塞原因。
- 命令被白名单拦截：改用已放行的 runner 命令，不得绕过策略。

## 每轮汇报最小格式
- 执行命令
- 远端结果（PASS/FAIL）
- 退出码
- 关键错误行（若失败）
- 下一步动作

## 完成标准
只有当远端执行验证通过，并完成要求的最终检查，任务才算完成。
